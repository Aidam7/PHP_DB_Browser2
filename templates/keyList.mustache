<table class='table table-striped'>

    <tr>
        <th>Místnost</th>
        <th>Identifikátor</th>
        <th>Administrace</th>
    </tr>

    {{#keys}}
        <tr>
            <td><a href="../room/detail.php?roomId={{room_id}}">{{room_name}}</a></td>
            <td>{{key_id}}</td>
            <td>
                <form action="../key/delete.php" method="post" style="display: inline-block">
                    <input type="hidden" name="employeeId" value="{{key_id}}">
                    <input type="submit" value="Smazat" class="btn btn-danger" onclick="return confirm('Opravdu chcete smazat klíč?\nAkce je nevratná!');">
                </form>
            </td>
        </tr>
    {{/keys}}
    {{^keys}}
        <tr><td colspan="3">Zaměstnanec nevlastní klíče</td></tr>
    {{/keys}}

</table>

<a href="../key/create.php" class="btn btn-primary">Založit nový klíč</a>